<div class="container-ingredients-index">
  <div class="d-flex justify-content-center align-items-center mt-3">
    <h1>What do we have?</h1>
  </div>
  <%= render 'shared/ingredients_card' %>
  <div class="card-add-ingredient">
    <div class="text-center mb-4 card-title">
      <h3>Add an Ingredient</h3>
    </div>
    <div class="form">
      <%= simple_form_for [@meal, @uploaded_ingredient], url: meal_uploaded_ingredients_path(@meal) do |f| %>
        <%= f.simple_fields_for :ingredient, @ingredient_new do |i_f| %>
          <%= i_f.input :name, label:false, placeholder: "Type ingredient here!", input_html: { class: "ingredient-input"} %>
        <% end %>
        <%= f.submit 'Add Ingredient', class: "mx-auto d-block btn-add-ingredient" %>
      <% end %>
    </div>
  </div>
  <%= link_to "Continue", edit_meal_path(@meal), class: "mx-auto d-block btn-continue" %>
</div>
